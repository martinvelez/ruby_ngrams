#!/usr/bin/ruby -w

require File.expand_path(File.join(File.dirname(__FILE__), "..", "lib", "ruby_ngrams"))

text = "Hello World"
for i in 1..text.size
	puts "n = #{i}"
	ngrams = Ngrams::extract(text, i, -999)
	puts ngrams
	puts "Count = #{ngrams.size}"
	puts "--------------"
end
